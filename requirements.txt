openai>=1.30.0
pyyaml>=6.0
python-dotenv>=1.0.0

# Inference engine for local model inside Docker
llama-cpp-python>=0.3.0

# Needed for Supra-Router-51M pre-screener (51M-param routing model, runs on CPU)
# These are not needed if use_supra: false in config/models.yaml
torch>=2.2.0
transformers>=4.40.0
